# 판금 견적 웹앱 PRD

> **상태**: 개발 진행 중 — 뷰어·절곡비 완료 (2026-06-28), 견적 통합 개발 예정  
> **작성일**: 2026-06-26 / **최종 수정**: 2026-06-28  
> **작성자**: 보기 (심플라인)

---

## 1. 개요

### 목적
DXF/STEP 도면을 업로드하면 재료비·가공비·후처리·관리비를 자동 산출하는 사내 판금 견적 웹앱.

### 배경
현재 견적은 수작업으로 산정 → 담당자 의존도 높고, 단가 기준이 문서화되어 있지 않음.  
도면에서 치수·형상 정보를 자동 추출하면 견적 시간을 단축하고 기준을 일관화할 수 있음.

### 성공 기준
- 도면 업로드 후 3분 이내 견적가 산출
- 수작업 견적 대비 오차 ±10% 이내 (단가 정립 후 검증)
- 단가표를 코드 수정 없이 웹 UI에서 편집 가능

---

## 2. 사용자 & 시나리오

### 주 사용자
심플라인 내부 견적 담당자 (설계 실무자 포함)

### 핵심 시나리오
1. 고객 문의 도면(DXF 또는 STEP)을 업로드
2. 재질·두께·수량·후처리 종류를 선택 (파싱으로 자동 입력, 필요 시 수정)
3. 절곡비 계산 방식 선택 (P4 패널벤더 / 일반 절곡기)
4. 견적 산출 결과를 화면에서 검토
5. PDF로 다운로드해 내부 보관 또는 고객 전달

---

## 3. 핵심 기능 요구사항

### F1. 도면 파일 업로드 & 파싱
| 항목 | DXF | STEP |
|------|-----|------|
| 전개면적 | 외곽 폴리라인 면적 | - |
| 순중량 기반 부피 | 전개면적 × 두께 × 비중 (근사) | 솔리드 부피 × 비중 (정확) |
| 절단장 | 외곽 + 구멍 둘레 합산 | - (미지원) |
| 구멍 수 | 원/호 엔티티 카운트 | - (미지원) |
| 절곡 횟수 | 절곡선 레이어 카운트 | 피처 수 (가능 시) |
| 표면적 | 전개면적 × 2 + 둘레 × 두께 (근사) | 솔리드 표면적 (정확) |

**파싱 실패 시**: 각 항목을 수동으로 직접 입력할 수 있는 폼 제공.

### F2. 재질·두께·후처리 선택 & 총액 자동 산출
- **재질**: SPCC, SUS304, SUS316, AL5052, AL6061 (추후 확장)
- **두께**: 0.8 / 1.0 / 1.2 / 1.5 / 2.0 / 2.3 / 3.0 / 4.5 mm (추후 확장)
- **후처리**: 없음 / 분체도장 / 도장(일반) / 도금
- **특수가공**: 버링업 / 엠보싱업 / 탭가공(M3·M4·M5·M6·M8) — 수량 직접 입력
- **절곡 방식**: P4 패널벤더 / 일반 절곡기 (사용자 직접 선택)
- **수량**: 정수 입력

**실시간 재계산**: 재질·두께·수량 등 입력값 변경 시 총액이 즉시 갱신 (페이지 이동 없음).  
DXF 파싱으로 자동 채워진 값은 수정 가능하며, 수정 즉시 금액에 반영.

### F3. 원가 산정 엔진

#### 재료비
```
재료비 = 순중량 × kg단가[재질]
순중량 (DXF) = 전개면적(㎡) × 두께(m) × 비중(kg/m³)
순중량 (STEP) = 부피(m³) × 비중(kg/m³)
```

#### 절단비
```
절단비 = 절단장(m) × 절단m단가[두께] + 구멍수 × 피어싱단가[두께]
```

#### 절곡비
```
절곡비 = Σ 절곡선별 tier단가  (bending_pricing.json 기반)
  - 절곡선 길이 0 ~ 500mm    : 150원/회
  - 절곡선 길이 500 ~ 1000mm : 250원/회
  - 절곡선 길이 1000mm 초과  : 350원/회
```
> 단가는 `/admin/pricing`에서 수정 가능. tier 구간도 향후 UI에서 편집 예정.

#### 도장/후처리비
```
표면적 (DXF) = 전개면적 × 2 + 외곽둘레 × 두께
표면적 (STEP) = 솔리드 표면적 (직접 추출)

후처리비 = 표면적(㎡) × 후처리㎡단가[후처리종류]
```

#### 관리비·마진
```
소계 = 재료비 + 절단비 + 절곡비 + 후처리비
관리비 = 소계 × 관리비율
마진 = 소계 × 마진율
합계 = 소계 + 관리비 + 마진
```

#### 수량 반영
```
최종 견적가 = 합계 × 수량
```
> 수량 할인 로직은 MVP 이후 검토.

### F4. 단가 관리 (관리자 UI + 인라인 편집)

**자동 로드**: 앱 시작 시 `pricing_seed.json`(재료·절단·후처리) + `bending_pricing.json`(절곡 tier)을 기본값으로 적용. DB 연결 시 DB값 우선.

**관리자 페이지** (`/admin/pricing`):
- 항목별 단가를 테이블 형태로 조회·수정·저장
- 단가 변경 이력 저장 (날짜 + 변경 전/후 값)

**견적 화면 인라인 편집**:
- 각 항목 단가를 견적 화면에서 직접 수정 가능 (관리자 페이지 이동 불필요)
- 수정 범위 선택: 해당 견적에만 적용(1회성) 또는 전역 저장

**단가 테이블 구조**

| 테이블 | 키 | 값 |
|--------|----|----|
| material_price | 재질 | kg단가 (원) |
| cut_price | 두께(mm) | m단가 (원/m) |
| pierce_price | 두께(mm) | 피어싱단가 (원/구멍) |
| bend_price (tier) | 길이구간(mm) | 회당단가 (원/회) |
| surface_price | 후처리종류 | ㎡단가 (원/㎡) |
| special_process_price | 가공종류 | 개당단가 (원/개) |
| overhead_rate | - | 관리비율 (%) |
| margin_rate | - | 마진율 (%) |

### F5. 견적서 출력 (PDF·인쇄)
- **화면**: 항목별 계산 내역 + 합계 표 (투명하게 공개)
- **출력 레이아웃**: 회사명·로고 / 견적 번호·날짜 / BOM 테이블 / 항목별 단가 내역 / 합계
- **구현**: `@media print` CSS 기반 브라우저 인쇄 우선 (별도 PDF 라이브러리 최소화)
- **톤**: 흰 배경 + 최소 장식, 수치와 테이블 중심 레이아웃
- PDF 파일명: `견적_[부품명]_[날짜].pdf`

### F6. BOM (부품 목록) 관리
- 뷰어에서 박스별 인식 결과 → BOM 행으로 자동 변환 (버튼 클릭 1회)
- **BOM 컬럼**: 순번 / 부품명 / 재질 / 두께 / 수량 / 재단방식 / 절곡수 / 절곡비 / 소계
- 부품 추가·삭제·순서 변경 가능
- BOM 전체 소계 합산 → 관리비·마진 적용 → 견적 총액
- 단품 도면도 BOM 1행으로 수동 추가 가능

### F7. 견적 저장 & 이력
- **저장**: 고객명 + 프로젝트명 + 날짜 + BOM 스냅샷 + 총액 저장
- **이력 목록**: 날짜순 조회, 클릭 시 재열람
- **재견적**: 저장된 견적 기반으로 단가·수량만 수정해 재계산 가능
- **DB**: Neon Postgres (기존 인프라 재사용, 별도 테이블 추가)

---

## 4. 기술 아키텍처

### 스택
| 레이어 | 기술 |
|--------|------|
| 프론트엔드 | Next.js 15 + TypeScript + Tailwind CSS |
| 백엔드 | Next.js API Routes (Server Actions) |
| DB | SQLite (로컬, Turso 또는 Vercel Postgres 고려) |
| ORM | Prisma 또는 Drizzle |
| DXF 파싱 | `dxf-parser` (npm) |
| STEP 파싱 | `opencascade.js` (WebAssembly) 또는 Python 마이크로서비스 |
| PDF 출력 | `@react-pdf/renderer` 또는 브라우저 인쇄 API |
| 배포 | Vercel (panel-mfg와 동일) |
| 인증 | 없음 (사내 접근 가정) |

### 데이터 흐름
```
도면 업로드
  → (서버) 파싱 → 형상 수치 추출
  → (클라이언트) 재질·두께·후처리·절곡방식 선택·수정
  → (서버) 단가 DB 조회 + 원가 공식 계산
  → 결과 표시 + PDF 다운로드
```

### STEP 파싱 결정 포인트 (미결)
`opencascade.js`는 번들 크기가 크고 초기화가 느림.  
대안: Python (pythonocc / CadQuery)으로 별도 마이크로서비스 구성.  
→ **MVP는 DXF 우선 구현, STEP는 2단계에서 결정.**

---

## 5. 비기능 요구사항

| 항목 | 목표 |
|------|------|
| 파싱 응답시간 | DXF ≤ 3초, STEP ≤ 10초 |
| 파일 크기 제한 | DXF ≤ 10MB, STEP ≤ 50MB |
| 브라우저 지원 | Chrome 최신, Edge 최신 |
| 인증 | 없음 (사내망 또는 Vercel URL 비공개로 접근 제한) |
| 단가 데이터 백업 | DB 파일 주기적 백업 (또는 클라우드 DB) |

---

## 6. 개발 범위 & 진행 상태

### 완료 (v1 — 2026-06-28)
- [x] DXF 업로드 & 파싱 (전개면적, 절단장, 구멍수, 절곡횟수)
- [x] DXF 뷰어 — SVG 렌더링, 레이어 토글, pan/zoom
- [x] 조립체 드래그 선택 & 영역 인식 (재질·두께·절곡수·재단길이)
- [x] 절곡비 tier 자동 계산 (`bending_pricing.json` 기반, `bending.ts`)
- [x] 단가 관리 페이지 구조 (`/admin/pricing`)
- [x] 뷰어 결과 패널 — 절곡비·절곡수·재단길이 요약 카드

### 다음 개발 (v1.5)
- [ ] 재료·두께·수량 입력 → 총액 실시간 산출 (F2+F3 통합)
- [ ] 표면처리·특수가공 옵션 선택 UI (F2)
- [ ] BOM 목록 뷰 — 인식 결과 → 부품 목록 (F6)
- [ ] 견적서 PDF·인쇄 출력 (F5)

### 이후 (v2)
- [ ] 견적 저장 & 이력 조회 (F7)
- [ ] 인라인 단가 수정 (F4)
- [ ] STEP 업로드 & 파싱
- [ ] 수량 구간 할인
- [ ] 고객사별 단가 프리셋

---

## 7. 미결 사항 (개발 전 확정 필요)

| 항목 | 현황 | 결정 방법 |
|------|------|-----------|
| 초기 단가표 값 | 미정 | 실무 견적 역산으로 정립 |
| DB 선택 (SQLite vs Postgres) | 미정 | 배포 환경 결정 후 |
| STEP 파싱 방식 | opencascade.js vs Python 마이크로서비스 | MVP 이후 |
| DXF 레이어 명명 규칙 | **완료** (8번 섹션 참조) | — |
| 수량 할인 정책 | 없음 (MVP 이후) | 실무 관행에서 추출 |

---

## 8. DXF 레이어 규칙 (2개 도면 교차 검증 완료 — 2026-06-27)

### 인코딩
- 파일 인코딩: 한글 (CP949 계열)
- 레이어명: 한글
- MTEXT 내 텍스트: `\U+XXXX` 유니코드 이스케이프 → 파서에서 디코딩 필요

### 레이어 용도표

| 레이어명 | 용도 | 엔티티 | 파싱 방법 |
|----------|------|--------|-----------|
| `외형선` | 부품 윤곽 + 구멍 | LINE / ARC / LWPOLYLINE / CIRCLE | 절단장·구멍 추출 |
| `굽힘선아래로` | 절곡선 (하향) | LINE | LINE 개수 = 절곡 횟수 |
| `굽힘선위로` | 절곡선 (상향) | LINE | LINE 개수 = 절곡 횟수 |
| `SW_노트` | 제목란 | MTEXT | 재질/두께/중량/수량 추출 |
| `숨은선` | 점선 형상 | - | **무시** |
| `SW_치수` / `SW_중심선` / `SW_테두리` | SolidWorks 자동 생성 | - | **무시** |
| `치수선` / `중심선` / `0` / `Defpoints` | 치수·기준점 | - | **무시** |

### 추출 항목별 방법

#### 재료 메타데이터 (SW_노트 MTEXT)
| 항목 | 파싱 패턴 | 예시 |
|------|-----------|------|
| 재질 | `SPCC\|SUS304\|SUS316\|AL5052\|AL6061` | `SPCC` |
| 두께 | `(\d+\.?\d*)\s*t\b` | `1 t` → 1.0 mm |
| 중량 | `(\d+\.?\d*)\s*kg` | `2.75 kg` |
| 수량 | `(\d+)\s*(ea\|pcs?)` | `1 ea / Set` |

#### 절단장 · 구멍 수 (외형선 레이어)
```
절단장 = Σ LINE 길이 + Σ ARC 호길이 + Σ LWPOLYLINE 길이 + Σ CIRCLE 둘레
구멍 수 = 외형선 CIRCLE 개수
```

#### 절곡 횟수
```
절곡 횟수 = 굽힘선아래로 LINE 수 + 굽힘선위로 LINE 수
```
- LINE 1개 = 절곡 1회 (병합 없이 단순 카운트)
- 위로/아래로 방향 각각 별도 기록, 합산이 총 절곡 횟수

#### 순중량 산정 (2단계 폴백)
1. **1순위**: SW_노트에서 중량(kg) 직접 읽기
2. **2순위**: 외형선 LWPOLYLINE 면적 × 두께 × 비중으로 역산

| 재질 | 비중 (g/cm³) |
|------|-------------|
| SPCC / SECC / SGCC / SS400 | 7.85 |
| SUS304 / SUS316 | 7.93 |
| AL5052 / AL6061 | 2.71 |

### 비표준 도면 처리 원칙
- 파서는 항목별 `추출 성공 / 미검출` 을 명시적으로 리포트
- 미검출 값은 `0`이 아닌 `null`로 구분 (0과 미검출은 의미가 다름)
- 표준 레이어(`외형선` / `굽힘선아래로` / `굽힘선위로` / `SW_노트`)가 하나도 없으면 → "비표준 도면" 판정, 오류 중단 아님
- 견적 화면에서 자동 추출값은 '기본 채움'으로 표시, 모든 항목 수정 가능
- 파서가 아무것도 읽지 못해도 전 항목 수동 입력으로 견적 완성 가능

### 가공 종류 (TAP / 버링 / 엠보) — MVP 제외
- 특수 가공은 도면 내 심볼로만 표기, 범례표(X ≈ 4570~4830 좌표 영역)에만 텍스트
- MVP: 외형선 CIRCLE을 단순 구멍 수로만 카운트, 가공 종류별 수량은 견적 화면에서 수동 입력
- 심볼 자동 인식은 v2 항목

### 검증 결과 (PoC 실행 — 2026-06-27)

| 도면 | 재질 | 두께 | 중량 | 수량 | 절곡(위/아래) | 절단장 | 구멍 |
|------|------|------|------|------|--------------|--------|------|
| Drawing2.dxf | SPCC | 1.0mm | 2.75kg (SW_노트) | 1 | 0/8 = 8회 | 21,732mm | 4개 |
| 하부장C 프레임판-01.dxf | SPCC | 1.0mm | 3.12kg (SW_노트) | 미검출 | 0/3 = 3회 | 16,615mm | 0개 |

> 절곡 횟수는 사전 검증값(Drawing2=8, 하부장C=3)과 완전 일치.

---

## 9. UI 디자인 원칙

| 항목 | 방향 |
|------|------|
| **레이아웃 구조** | 도면 미리보기 캔버스(좌/중앙) + 입력·단가·BOM·합계 작업 패널(우/주변) **2영역 분할** |
| **도면 캔버스 영역** | 다크 배경 `#0f172a` + 밝은 레이어 선. SolidWorks·AutoCAD 뷰포트 참조. **이 영역에만 다크 적용** |
| **작업 패널 영역** | 흰색~`slate-50` 밝은 배경. 입력폼·단가표·BOM·합계 등 모든 데이터 입력·조회 영역. **전체 화면 다크 금지** |
| **포인트 컬러** | `blue-600` 단일 포인트, 소규모 강조에만 사용. 의미색: 절곡비 `emerald` · 재단 `blue` · 재료 `slate` |
| **정보 계층** | 요약 숫자 카드(크게) → 상세 테이블(아래). 장식 최소화, 수치와 테이블이 1순위 |
| **수정 용이성** | DXF 파싱 자동값은 기본 채움, 모든 입력란 수동 수정 가능 |
| **출력(PDF·인쇄)** | 흰 배경 그대로 유지. `@media print` CSS 분리, 캔버스 다크 스타일 미적용 |
