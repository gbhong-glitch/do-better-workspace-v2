# 판금 견적 웹앱 — 진행 상황

최종 업데이트: 2026-06-28

## 완료한 것

- PRD.md 업데이트 완료(커밋 0bf1f88): 기능 7개(실시간 재계산, 단가표 연동, 인라인 단가 수정, 표면처리·특수가공, BOM 관리, PDF 견적서 출력, 견적 이력·저장) + UI 디자인 원칙 추가
- UI 디자인 핵심 결정: 전체 다크 금지. 도면 미리보기 캔버스만 다크(#0f172a), 입력·단가·BOM·합계 작업 패널은 밝은 배경(SolidWorks/AutoCAD 방식). 출력(PDF·인쇄)은 흰 배경.
- Stitch 디자인을 web 프로젝트에 3단계로 구현 완료: globals.css 디자인토큰 → layout.tsx 헤더/폰트 → page.tsx 좌 다크캔버스+우 밝은패널 분할레이아웃. localhost:3000에서 실제 화면 확인함.
- zoom 기준점 수정(onWheel, getScreenCTM 커서 고정 확대) 완료
- viewer 결과 패널 텍스트 선택 방지(user-select:none) 완료

## 절곡 인식 규칙 확정 (2026-06-28)

- 굽힘선 LINE을 (방향 레이어 + 직선위치) 키로 그룹핑, 그룹 1개 = 1곡
- 같은 직선+같은 방향(collinear, 캐드 구름표시) → 1곡으로 합침
- 같은 직선이라도 방향(위로/아래로) 다르면 → 별개 곡 (반대 절곡)
- `bendKey()` 헬퍼 구현, `totalBends=bendGroupMap.size`, 부품별 bendDown/bendUp도 그룹 수
- `bendGroupLengths`로 그룹당 합산 길이 저장 (tier 단가용)
- 검증: 연결형 하부장 커버판-01 LINE 12개 → 8곡 정답 일치

- B방식 견적 단가 자동 연동 완료: pricing_defaults.json 분리, viewer가 인식 재질/두께로 재료·절단·피어싱 단가 자동 채움(parts에서 재질있는 부품 탐색, 두께 1t→1.0 정규화)

## 다음 할 일

- zoom 기준점 수정(onWheel), 레이저/복합기 견적금액 연결
- 견적금액 연결 완료(A방식): 견적 입력 섹션 추가, 재료비(중량×재료단가)+절단비(재단길이×절단단가+구멍수×피어싱단가)+절곡비=합계 실시간 계산, 탭별 입력값 독립 유지(quoteInputs)

## 절곡수 버그 디버깅 중 (2026-06-28)

- 증상: 부품 1번(복합기)이 8곡이어야 하는데 6곡으로 나옴. 2곡 누락.
- 곡수 로직(bendKey, bendGroupMap.size, collinear 그룹핑)은 코드에 정상 존재 (커밋 3e0b47d). UI 재설계 작업은 이 로직 안 건드림(git diff 확인).
- recognizer.ts 그룹핑 부분(220~234줄)에 [진단] console.log 임시 추가한 상태.
- 다음: 콘솔 로그가 조립체-01/02 섞여 찍혀 부품 구분 안 됨. 부품별로 로그 분리해서 커버판-01의 굽힘선 LINE 좌표/key를 봐야 함. 어느 절곡 2개가 잘못 처리되는지(누락 or 오병합) 확인 필요.
- UI 재설계(Stitch 시안, 3존 레이아웃)는 적용 완료, 아직 미커밋 상태.

## 참고

- 1차 시도 실패 교훈: 색상만 다크로 바꾸면 레이아웃이 그대로라 "제조 전문 툴" 느낌이 안 남. 그래서 Stitch로 레이아웃부터 새로 그리는 방식으로 전환.
