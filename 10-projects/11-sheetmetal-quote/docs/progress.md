# 판금 견적 웹앱 — 작업 진행 현황

최종 업데이트: 2026-06-29

---

## 2026-06-29 엑셀 내보내기 기능 완료

- xlsx(SheetJS) 패키지 설치
- `lib/export-excel.ts` 생성 — 요약 시트(조립체명/부품수/소계/전체합계) + 조립체별 시트(품명·재질·두께·곡수·재단·수량·재료비·절단비·절곡비·후처리비·단가·금액)
- `/result` 페이지에 "엑셀 다운로드" 버튼 추가 (클라이언트 사이드, 서버 API 불필요)
- 타입체크(`tsc --noEmit`) 통과, 파일명 `estimate_YYYY-MM-DD.xlsx`
- 커밋: 03a2231, 1a152cb, 84a84ad / push 완료

---

## 2026-06-29 부품별 견적서 완성 (브라우저 검증 완료)

### 완성된 기능
- **부품별 견적서**: 조립체별 섹션 > 품명별 행 > 행 클릭 시 상세 펼치기(재료비/절단비/절곡비/후처리비/관리비/마진) > 조립체 소계 > 전체 합계
- **뷰어 → 견적서 흐름**: `/viewer`에서 박스 인식 후 "견적서 생성 →" 버튼 → `/api/estimate-multi` → `/result` 부품별 표시 정상 동작
- **공통 설정**: 뷰어 패널 상단에 절곡방식(P4/일반) + 후처리 드롭다운 추가
- 단가표 더미값 반영(커밋 99d6ee8) 및 PDF 다운로드 버튼(커밋 cd228e2) 기완료

### 검산 결과 (예제.dxf, P4 패널벤더, 분체도장)
| 조립체 | 금액 |
|--------|------|
| 조립체-01 | ₩70,125 |
| 조립체-02 | ₩145,314 |
| **전체 합계** | **₩215,439** |

### 추가한 파일·함수
| 위치 | 내용 |
|------|------|
| `lib/estimate.ts` | `calculatePartEstimate()` / `calculateMultiPartEstimate()` / `getDensity()` / 3개 타입 추가 |
| `app/api/estimate-multi/route.ts` | 부품별 견적 API 엔드포인트 (신규) |
| `app/viewer/page.tsx` | 설정 드롭다운 + "견적서 생성 →" 버튼 |
| `app/result/page.tsx` | MultiPartView / SinglePartView 분기 렌더링 |

## 다음 할 일
1. **실제 업체 단가표** 받으면 `pricing_defaults.json` 교체
2. **비중 통일**: `getDensity(AL)` 2.71/2.70 혼용 중 → 2.71로 통일
3. 다른 도면 2~3개로 부품별 견적 흐름 일반화 검증

---

## 2026-06-29 진행상황
- 부품 인식을 CUT라벨 → SW_노트 품명 기준으로 변경 완료 (빈 행 제거, 6부품 30곡)
- 굽힘선→품명 배정을 band(Y구간) → 2D 최근접(Math.hypot)으로 교체 완료, 타입 오류 없음
- [미검증] 브라우저에서 결과가 이전과 동일하게 나옴 (01=7, 02L=6, 03=6) → 캐시 또는 dev서버 미반영 의심

## 내일 할 일
1. 브라우저 Ctrl+Shift+R 강력 새로고침 후 예제.dxf 드래그
2. 안 바뀌면 npm run dev 재시작
3. 기대값: 01=8, 02L=5, 02R=5, 03=8, 04=2, 05=2 / 합계 30곡
4. 통과하면 커밋+push

---

## 2026-06-29 굽힘선 배정 규칙 확정 (실무자 검증 완료)

- **부품 구분 = SW_노트 품명 기준**: "품명 바로 위에 있는 전개도의 굽힘선 = 그 부품의 절곡"
- 현재 `recognizer.ts`는 "레이저/복합기 라벨까지 2D거리"로 배정 → 라벨이 부품과 멀리(별도 칼럼) 떨어져 있어 전체 드래그 시 경계 굽힘선 6곡이 샘
- **고칠 방향**: 배정 기준을 "재단라벨 거리" → "SW_노트 품명 위쪽 전개도"로 변경
- 곡수 그룹핑(`bendKey`, 방향 분리)은 이미 정상 — 예제.dxf 12부품 66곡 전부 실무 기준과 일치 검증함 (하부장판-01=8곡, 커버판-01=8곡, SMPS단자함-02=6곡 등)
- 다음 세션: `recognizer.ts` 배정부를 품명 기준으로 재작성. 곡수 세는 로직은 건드리지 말 것.

---

## 2026-06-29 굽힘선 배정 재설계 — 원인 확정 + 방향 결정

- **근본 원인**: Pass1이 거리 기반으로 lbbox를 만들고, Pass2가 그 lbbox로 배정 → 초기 배정이 틀리면 bbox도 오염 (닭·달걀 구조). 경계 굽힘선은 이웃 라벨에 더 가까워 Pass1에서 엉뚱한 bbox로 흡수됨.
- **재설계 방향**: 라벨 위치 + 외형선 bbox로 부품 영역을 **먼저** 확정 → 굽힘선을 포함 여부로 1-pass 배정 (거리 휴리스틱 제거)
- **착수**: 오늘 세션에서 `recognizer.ts` 배정 로직 재작성 예정

---

> **지시6 완료**: 절곡비 자동 계산 step1~4 끝 (bendLengths 인식 → bending.ts 단가 모듈 → estimate.ts 연결 → 뷰어 표시)

---

## 2026-06-28 자재비 견적 기능 완성

- `RecognizedPart`에 `widthMm`/`heightMm` 추가 (외형선 bbox 기준) — 커밋 e1950b0
- 부품 중량 자동계산 (소재면적 × 두께 × 비중) + 재료비 견적 반영 — 커밋 f38e31d
- viewer 표에 가로/세로(mm) 열 추가
- 단가는 `pricing_defaults.json` 기본값(1800/450/60) 사용 중, 실제 거래처 단가표로 교체 필요
- 검증: 예제.dxf 기준 중량 3.99kg, 재료 ₩7,183 + 절단 ₩4,631 + 절곡 ₩4,000 = 총 ₩15,814 정상 출력
- 보류: 굽힘선 미배정(전체드래그 시 6곡) 버그 — 표시 정리 안 함

---

## 완성된 기능 (지시 1~5)

### 지시 1 · DXF 뷰어
- DXF 파일 업로드 → SVG 렌더링 (LINE / ARC / CIRCLE / LWPOLYLINE / SPLINE / MTEXT / INSERT)
- 레이어별 색상 구분, pan/zoom (핀치 포함), `vector-effect="non-scaling-stroke"` 1px 선
- Y축 플립 (`matrix(1,0,0,-1,0,K)`) — DXF Y-up → SVG Y-down 정규화

### 지시 2 · 드래그 조립체 선택
- 모드 전환: 이동 ↔ 선택
- 드래그로 여러 박스 생성, 박스마다 이름 편집 가능
- 스크린 픽셀 → DXF 좌표 역변환으로 박스 좌표 계산
- `selectStart.current` stale closure 버그 수정 (로컬 변수 캡처)

### 지시 3 · 영역 인식 (parse_simpleline.py → TypeScript 이식)
- 재단방식 레이어(레이저/복합기/NCT/절단) TEXT → 세부부품 라벨 생성
- 절곡선 → Y최근접 라벨 자동 배정 (굽힘선아래로 / 굽힘선위로)
- 재단길이 = 절곡 바운딩박스 ±50mm 안 외형선 합계
- SW_노트 → 재질(MAT_RE) / 두께(THICK_RE) / 수량(QTY_RE) 추출
- INSERT 블록 이름 분류 → 버링업 / 엠보싱업 / 버링탭 / 탭가공 / 자석부착 / 러버
- layer-0 TEXT 파이프 절단 표(`품번` 헤더 기준)
- 박스 생성 시 클라이언트 사이드 동기 실행 (API 왕복 없음)

### 지시 4 · 인식 결과 테이블
- 하단 패널 (240px): 박스별 탭 전환
- 부품 테이블: 순번 / 재단방식(배지) / 절곡↓(red) / 절곡↑(orange) / 합계 / 재단길이(blue) / 재질 / 두께 / 수량
- 합계 행: 총 절곡수 + 미배정 카운트
- 특수가공 인라인 표시, 파이프 절단 테이블

### 지시 5 · 단품 도면 대응 & 검증
- **문제 발견**: 샘플 파일(Drawing2, 하부장C)은 레이저/복합기 레이어 없는 단품 전개도
  - SW_노트(SPCC, 1t)가 도면 우측 제목란에 위치 — 박스에서 ~2000mm 떨어짐
  - `하부장C` 수량 `"ea / Set"` (숫자 없음) → QTY_RE 미매칭 (실제 미기재, 정상)
- **수정 ①** `recognizer.ts` — SW_노트 전역 폴백: 200mm 여유 박스 내 미검출 시 도면 전체 검색
- **수정 ②** `recognizer.ts` — 단품 폴백: 재단 라벨 없고 절곡/외형선 있으면 부품 1개로 생성, 외형선 전체 합을 재단길이로 사용
- **수정 ③** `viewer/page.tsx` — 단품 도면 안내 배너 + 재단방식 수동 드롭다운 (레이저/복합기/NCT/절단)

---

## 만든 파일

| 경로 | 역할 |
|------|------|
| `web/app/viewer/page.tsx` | DXF 뷰어 UI — 렌더, 드래그, 인식 결과 패널 |
| `web/app/api/viewer/route.ts` | POST /api/viewer — DXF → ViewerData JSON |
| `web/lib/dxf-viewer.ts` | DXF 파서 (렌더링용) — DrawEntity 추출 |
| `web/lib/recognizer.ts` | 박스 영역 인식 엔진 — parse_simpleline.py 이식 |
| `web/scripts/test-parser.ts` | 파서 검증 스크립트 (Drawing2 / 하부장C 대조) |
| `samples/parse_dxf.py` | Python PoC — 지상 검증용 |
| `samples/parse_simpleline.py` | Python 원본 — 인식 알고리즘 레퍼런스 |

---

## 2026-06-28 — 전체 선택 굽힘선 오배정 디버깅 (미해결, 세이브포인트 복귀 예정)

### 현상
- **단품 선택** (부품 1개 드래그): 절곡수 정상 인식
- **전체 드래그 선택** (부품 7개 조립체): 굽힘선 30절곡 전부 미배정 또는 오배정 (예: 8절곡 부품이 6절곡+2미배정)

### 원인 — 기존 2-pass 거리 기반 배정의 한계
격자(grid) 배치 도면에서 **경계 굽힘선이 이웃 부품 라벨에 더 가까울 수 있어** 오배정 발생.
기존 로직: Pass1(가장 가까운 라벨에 배정 → bbox 확장) → Pass2(bbox 밖 굽힘선 2D 거리 재배정)

### 시도: FORMAT 셀 bbox 1-pass 배정

FORMAT 레이어의 사각형 칸이 부품 1개에 정확히 대응 → 셀 bbox로 1-pass 배정을 시도.

**시도 1 — 닫힌 LWPOLYLINE 감지**: FORMAT 칸 테두리가 개별 LINE 4개로 그려짐 (LWPOLYLINE 없음) → 실패

**시도 2 — FORMAT LINE 그리드 교차점 방식**: 수평/수직 FORMAT LINE의 교차점으로 셀 생성

### FORMAT 레이어 좌표 구조 (Python DXF 직접 파싱으로 규명)

| 영역 | X 범위 | Y 범위 | 비고 |
|------|--------|--------|------|
| 부품 전개도 격자 | X=0~7000 | Y=1000~13000 | 7행 × 3열, H선 2000mm 간격 |
| BOM 표 격자 | X=9000~16000 | Y=4989~12989 | H선 Y값이 부품칸보다 11mm 오프셋 |
| 제목란 장식선 | X=4701~5000 | Y=3380~3474 | 단선 44개, 최장 485mm |

- 굽힘선 실제 위치: X=700~2000, Y=1944~12064
- 부품 전개도 V선: X=0, 5000, 6000, 7000 / BOM 표 V선: X=9000~16000 혼재

### 파라미터 시도 결과

| 조합 | 결과 | 문제 |
|------|------|------|
| formatLineBox ±500mm 마진 | 셀 32개, X:3499~7001 | X=0 경계선이 마진 밖으로 잘려 부품칸 미생성, BOM셀만 남음 |
| 마진 제거 + L≥500mm + ε=20mm | 미검증 | 파라미터 조정 후 단품 폴백까지 손상됨, 동작 불완전 |

### 현재 코드 상태
- `recognizer.ts` FORMAT 1-pass 배정 wip 상태 (검증 실패)
- 세이브포인트 커밋: `wip: FORMAT 셀 경계 수정 직전 세이브포인트`
- **다음 세션: 세이브포인트로 복구 후 재접근**

### 다음 방향 후보
- **(a) BOM표 X 분리**: X=7000 기준으로 FORMAT V선 좌/우 분리 → 부품칸(X≤7000)만 추출
- **(b) 그리드 포기**: FORMAT 선 대신 다른 부품 경계 인식 방식 탐색
- **(c) 2-pass 폴백 복구**: 라벨 거리 배정 유지 + 격자 오배정만 별도 보정

---

## 다음 할 일

1. **굽힘선 오배정 버그** — 세이브포인트 복구 후 방향 (a)/(b)/(c) 중 재접근
2. **Vercel Neon DB 연결** — Vercel 대시보드에서 Neon Postgres 생성 → `.env.local` 설정 → `npm run db:migrate` 실행
3. **실제 단가 입력** — `/admin/pricing` 에서 재질/두께/공정별 단가 입력 (`pricing_seed.json` 값 현재 전부 0)
4. **견적 계산 연동** — 인식 결과(절곡수, 재단길이, 재질, 두께, 수량) → 원가 공식 적용 → 결과 패널에 금액 표시
5. **PDF 다운로드** — 결과 페이지 버튼 연결 (UI 준비 완료, 구현 미완)
6. **STEP 파싱** — MVP 이후 단계

---

## 2026-06-29 (이어서) - 굽힘선 배정 버그 완전 해결
- 문제: 여러 부품 동시 드래그 시 옆 부품 굽힘선이 새던 버그
- 원인 추적:
  - band(Y구간) 방식 → dx 무시해서 옆 부품 곡 흡수
  - FORMAT 격자 활용 시도 → FORMAT은 도면 양식 텍스트라 칸 경계 아님 (사용 불가)
  - 외형선 클러스터링(이진탐색으로 클러스터수=품명수) 시도 → 클러스터 bbox 겹침으로 03이 04·02R 흡수
- 최종 해결: 클러스터링 전부 제거(~80줄), 굽힘선 그룹마다 모든 nameAnchor와 2D 거리(Math.hypot) 계산 → 최근접 품명에 직접 배정
- 검증: 좌영역 01=8, 02L=5, 02R=5, 03=8, 04=2, 05=2 = 30곡 / 단일 선택(01=8)도 정확
- 커밋: dd6aa4b, ui-redesign 브랜치 push 완료
- 보존: bendKey/bendGroupMap(곡수 세기) 무손상
- 단가표 반영(더미값) 완료, 예제 견적 총액 155,117원 정상

## 다음 할 일
- 실제 업체 단가표를 pricing_defaults.json에 반영
- PDF 다운로드 기능 구현 (UI는 있으나 미구현)
- 다른 도면 2~3개로 nameAnchor 2D 최근접 방식 일반화 검증

---

## 2026-06-30 P4 자동생성 3단계 완료

P4 자동생성 3단계 완료 - bend-sequence.ts(펼침여유 계산+면배정+절곡순서), route.ts에 bendSequence 추가. 하부장 커버판-02로 검증: 8곡→4면 배정, 각 면 내측먼저 순서, 평판 1001.6x359.6 계산 확인. 타입체크 통과. 다음은 4단계 P4 텍스트 생성기(p4-generator.ts).

### 완성된 P4 자동생성 단계
| 단계 | 파일 | 내용 |
|------|------|------|
| 1단계 ✅ | `samples/P4/` | .P4 예제 21개 + PDF 매뉴얼 보관 |
| 2단계 ✅ | `lib/p4/dxf-bend-parser.ts` | SW_TABLEANNOTATION_0 BOM 파싱 + 굽힘선 그룹 추출 + 위치 기반 매칭 |
| 3단계 ✅ | `lib/p4/bend-sequence.ts` | 평판 bbox 계산 + L값 산출 + 4면 배정 + 절곡 순서 결정 |
| 4단계 🔜 | `lib/p4/p4-generator.ts` | P4 텍스트 생성 (COD/DIM/REF/MCM/POS/ROT+BEN/END) |
| 5단계 🔜 | UI 연결 | 결과 화면 "P4 생성" 버튼 + 미리보기 + 다운로드 |

### 검증 결과 (하부장 커버판-02.DXF, 8곡)
- BOM: A~H 8행 전부 추출 (방향=아래, 각도=90°, 반경=0.1mm)
- 절곡선 그룹: 수평(H) 4개 940mm + 수직(V) 4개 298mm
- 면 배정: top/left/right/bottom 각 2곡씩
- 각 면 내측(L=30.6mm) → 외측(L=14.2mm) 순서
- 전개도 치수: 1001.6mm × 359.6mm
- API: `POST /api/p4-parse` → `{ bom, bendGroups, bendSequence }` 반환
